/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: {
      unoptimized: true,
    },
    assetPrefix: "/music-predictor-pages",
  };
  
export default nextConfig;